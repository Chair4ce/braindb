# BrainDB Single-Node Configuration
# Copy to .env and customize

# Neo4j credentials (password auto-generated by setup.sh)
NEO4J_USER=neo4j
NEO4J_PASSWORD=CHANGE_ME

# Memory settings
NEO4J_HEAP=256m
NEO4J_PAGECACHE=128m

# Gateway port (localhost only — not exposed to network)
GATEWAY_PORT=3333

# Optional: API key for gateway authentication
# If set, all requests must include header: Authorization: Bearer <key>
# Leave empty to disable (safe when bound to localhost)
# BRAINDB_API_KEY=
